/** @jsx createElement */
import {
    // Component,
    // createElement,
    // render,
    // lazy,
    // Suspense
  } from '../src'
describe('forwardRef', () => {
  it('should render only if props change or not equal', () => {
    // const LazyComponent = lazy(() => import('./lazyComponent'))
    // class App extends Component {
    //   constructor (props) {
    //     super(props)
    //     this.state = {
    //     }
    //   }
    //   render () {
    //     return (
    //       <LazyComponent.Suspense fallback={<div>loading ...</div>}>
    //         <LazyComponent />
    //       </LazyComponent.Suspense>
    //     )
    //   }
    // }
    // const div = document.createElement('div')
    // document.body.appendChild(div)
    // render(<App />, div)
  })
})
